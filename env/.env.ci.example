# =============================================================================
# dashtam-jobs CI Environment Configuration
# =============================================================================

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# Database Configuration (CI uses in-memory or mock)
DATABASE_URL=postgresql+asyncpg://ci_user:ci_password@postgres:5432/ci_test

# Job Result Configuration
JOB_RESULT_TTL=300

# Worker Configuration
WORKER_CONCURRENCY=2

# Logging Configuration
LOG_LEVEL=INFO

# Token Expiry Check Configuration
TOKEN_EXPIRY_THRESHOLD_HOURS=24
TOKEN_CHECK_CRON=*/15 * * * *
